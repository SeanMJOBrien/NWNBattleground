/*
  To build a module from thisGit repository, simply create a folder in your
Modules folder and give it the desired module name. USe the NWToolset to open
the omdule/folder and it will ask if you want to open the folder, say Yes.
Then simply save the modules and it will be both a folder and module.
Be sure to run a full build (Used) before you save the module.
*/
